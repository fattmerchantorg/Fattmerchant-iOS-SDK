# Track large frameworks with Git LFS
# Users without Git LFS will get pointer files instead of the actual framework
# This allows the repository to include CloudCommerce without requiring all users to have LFS
OptionalFrameworks/**/*.xcframework/** filter=lfs diff=lfs merge=lfs -text
